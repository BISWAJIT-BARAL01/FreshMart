// Replaced Firebase SDK with placeholders for PHP Backend migration.
// This file is kept to avoid breaking imports in un-refactored files.

export const app = null;
export const auth = null;
export const db = null;
export const storage = null;
export const googleProvider = null;
